[
    {
        "type": "function",
        "function": {
            "name": "rag_query",
            "description": "Queries a private RAG API to retrieve data from secure documents.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "The query to send to the RAG API."
                    }
                },
                "required": ["query"]
            }
        }
    }
]
